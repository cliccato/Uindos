package utils;

import java.io.File;

public class UindosDirectoryName {
    public static final String DIRECTORY_FILE_DI_TESTO = "File di testo/".replace("/", File.separator);
    public static final String DIRECTORY_BACKGROUND_DESKTOP = "Background/".replace("/", File.separator);
    public static final String DIRECTORY_IMMAGINI_PAINT = "Immagini paint/".replace("/", File.separator);
    public static final String DIRECTORY_FOTO = "Foto/".replace("/", File.separator);
}